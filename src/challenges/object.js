/**
 * 1) Create a JavaScript object named student with the following properties:
name (string): Set it to 'Alice Smith'.
age (number): Set it to '20'.
grade (string): The student's grade level is 'Sophomore'.
school (string): Set it to 'Example High School'.
courses (array of strings): An array of course names 'Mathematics', 'Science', and 'History'.
2) Ensure that the student object contains the properties as described above.
3) Print each key into console.log() to see the output. */

const student = {
    name: "Alice Smith",
    age: 20,
    grade: "Sophomore",
    school: "Example High School",
    courses: ["Mathematics","Science","History"]
}

// Print each key into console.log() to see the output
console.log(student.name);
console.log(student.age);
console.log(student.grade);
console.log(student.school);
console.log(student.courses);
