/**
An array is a data structure that contains a group of elements
Typically these elements are all of the same data type such 
as integer or string
are commonly used in computer programs to organize data 
so that related set of values can be easily sorted or searched 
 */

// create an array access array item using the index position

const fruits = ["Apple", "Banana", "Pear", "Kiwi"];
console.log(fruits); 