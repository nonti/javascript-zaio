/**
 * 
 Your task is to create the template literal and insert the data in the appropriate places to generate the message as specified. The message should be a single string, and you should use template literals to achieve this.
Instructions:
1) Create a template literal that generates a message based on the provided data. You need to use the following data:
User's first name
User's last name
User's age
User's favorite color
2) Generate a message in the following format:
Hello, [First Name] [Last Name]! You are [Age] years old and your favorite color is [Favorite Color].
3) Replace [First Name], [Last Name], [Age], and [Favorite Color] with the actual values from the provided data.
*/
// Modify the code and print the appropriate message based on the test case shown below.
// Given data

let firstName = "John";
let lastName = "Doe";
let age = 30;
let favoriteColor = "Blue";

// Your code here
console.log(`Hello, ${firstName} ${lastName} ! You are ${age} years old and your favorite color is ${favoriteColor}.`);